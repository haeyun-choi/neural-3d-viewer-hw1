# Vite dev and Docker Compose already proxy /api. An external API may be set here.
VITE_API_BASE_URL=/api
# auto: try database once, fall back visibly to localStorage. local: static only.
VITE_STORAGE_MODE=auto
